<section class="section py-0">
  <h1 class='is-size-3 has-text-centered'>Friends</h1>
</section>

<%= render partial: 'users/listed_user', collection: @friends, as: :user %>

<%= render layout: 'shared/page_ranges', locals: { page_ranges: @page_ranges } do |page| %>
  <% friends_path(page: page != 1 ? page : nil) %>
<% end %>