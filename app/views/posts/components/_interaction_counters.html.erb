<div class="is-flex is-justify-content-space-between">
  <span>
    <%= turbo_frame_tag post.container_id(:likes_count) do %>
      <%= post.likes.size %> Likes
    <% end %>
  </span>
  <span>
    <%= turbo_frame_tag post.container_id(:total_comments_count) do %>
      <%= post.total_comments_count %>
    <% end %>
    Comments
  </span>
</div>